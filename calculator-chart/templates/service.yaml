apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}

spec:
  selector:
    app: calculator

  ports:
  - port: {{ .Values.service.port }}
    targetPort: 5000

  type: {{ .Values.service.type }}